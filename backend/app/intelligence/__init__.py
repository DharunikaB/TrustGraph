"""TrustGraph M2 Intelligence Engine.

Turns observable M1 data into graph-derived candidate clusters and
structured evidence. See `pipeline.py` for the architectural boundary
and `pipeline.run_pipeline` / `pipeline.run_pipeline_async` for the
entrypoints.
"""
